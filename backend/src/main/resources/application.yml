spring:
  datasource:
    url: jdbc:mysql://mysql:3306/meeting_app?serverTimezone=Asia/Seoul
    username: root
    password: 3251
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080