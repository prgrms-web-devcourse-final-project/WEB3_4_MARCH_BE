import AppScreenLayout from "../layout/AppScreenLayout";

type ProfileActivityProps = {
  params: {};
};

const ProfileActivity: React.FC<ProfileActivityProps> = ({ params: {} }) => {
  return <AppScreenLayout>Profile</AppScreenLayout>;
};

export default ProfileActivity;
